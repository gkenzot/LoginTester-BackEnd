# ========================
# CONFIGURAÇÕES PRINCIPAIS
# ========================
spring.application.name=login-auth-api-prod

# ========================
# DATABASE (MySQL)
# ========================
spring.datasource.url=jdbc:mysql://localhost:3306/logintester_db
spring.datasource.username=logintester_user
spring.datasource.password=SenhaSegura123@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========================
# JPA/HIBERNATE
# ========================
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ========================
# SERVER & HTTPS
# ========================
server.port=8443

# SSL Config
server.ssl.enabled=true
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=123357kk
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=myapp

# CORS Config
app.cors.allowed-origins=https://seu-dominio-producao.com
app.cors.allowed-methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
app.cors.max-age=3600

# ========================
# JWT
# ========================
jwt.secret-key=w8L#zT!qVp@7sKXg$B2^mNyAe%Uc39rF*1Dj
jwt.expiration=86400000

# ========================
# OUTROS
# ========================
# Swagger Config
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false 